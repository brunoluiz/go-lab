version: "2"
sql:
  - engine: "postgresql"
    queries: "./internal/db/queries/lists.sql"
    schema: "./internal/db/migrations"
    gen:
      go:
        package: "lists"
        out: "./gen/sqlc/lists"
        emit_interface: true
