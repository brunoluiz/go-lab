description: API JSend Response Wrapper
content:
  application/json:
    schema:
      required:
        - status
      type: object
      properties:
        status:
          type: string
        message:
          type: string
        data:
          type: object
          $ref: '#/components/schemas/DataResponse'
